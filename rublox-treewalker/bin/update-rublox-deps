#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

pushd ../rublox-parser
rm -rf pkg
rake build
popd

pushd ../rublox-test
rm -rf pkg
rake build
popd

for gem in ../rublox-parser/pkg/*.gem; do
  gem install "$gem"
done

for gem in ../rublox-test/pkg/*.gem; do
  gem install "$gem"
done

#!/usr/bin/env ruby

$LOAD_PATH.unshift("#{__dir__}/../lib")

require "rblox/bytecode"

def read_bool_env_var(variable)
  value = ENV[variable]
  !value.nil? && !value.empty? && !["0", "false"].include?(value.downcase)
end

debug_mode = read_bool_env_var("RBLOX_DEBUG_MODE")
stress_gc = read_bool_env_var("RBLOX_STRESS_GC")

if ARGV.length > 1
  puts "Usage: rblox-bytecode [script]"
  exit 64
elsif ARGV.length == 1
  file_path = ARGV[0]
  contents = File.read(file_path)
  main = Rblox::Bytecode::Main.new(debug_mode: debug_mode, stress_gc: stress_gc)
  main.run(contents)
  exit 65 if main.had_error?
  exit 70 if main.had_runtime_error?
else
  Rblox::Bytecode::Repl.new($stdin, Rblox::Bytecode::Main.new(debug_mode: debug_mode, stress_gc: stress_gc)).run
end
